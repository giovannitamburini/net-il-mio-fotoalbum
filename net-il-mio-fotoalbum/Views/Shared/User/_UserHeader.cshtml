<nav class="navbar navbar-expand-lg bg-body-tertiary border-bottom border-2 border-black">
    <div class="container-fluid">

        <a class="navbar-brand" href="@Url.Action("Index", "Home")">
            <img src="~/img/portfolio.png" alt="portfolio" width="55" class="d-inline-block align-text-top">
        </a>

        <a class="navbar-brand" href="@Url.Action("Index", "Home")">PORTFOLIO</a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="@Url.Action("Index", "Home")">HOME</a>
                </li>

                @* mostro il link della index di amministrazione solo se sei loggato come ADMIN *@
                @if (User.IsInRole("ADMIN"))
                {
                    <li class="nav-item">
                        <a class="nav-link" href="@Url.Action("Index", "Photo")">Admin Home</a>
                    </li>
                }
                <li class="nav-item">
                    <a class="nav-link" href="@Url.Action("Send", "Home")">Contattaci</a>
                </li>
            </ul>
        </div>
    </div>
</nav>